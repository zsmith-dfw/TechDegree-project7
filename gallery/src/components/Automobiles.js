import React from 'react';

const Automobiles = (props) => (
  <div className="main-content">
    <h2>{ props.title }</h2>
    <p>This will show photos of automobiles</p>
  </div>
);

export default Automobiles;